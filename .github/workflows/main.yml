name: Build macOS VST3

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: macos-14
    steps:
      - name: Checkout plugin
        uses: actions/checkout@v4
        with:
          path: myplugin

      - name: Clone VST3 SDK
        run: |
          git clone --depth 1 --recurse-submodules \
            --shallow-submodules \
            https://github.com/steinbergmedia/vst3sdk.git vst3sdk

      - name: Copy plugin into SDK
        run: |
          mkdir -p vst3sdk/my_plugin
          cp -r myplugin/src vst3sdk/my_plugin/
          cp -r myplugin/include vst3sdk/my_plugin/
          cat >> vst3sdk/CMakeLists.txt << 'EOF'
          add_subdirectory(my_plugin)
          EOF

      - name: Write plugin CMakeLists
        run: |
          cat > vst3sdk/my_plugin/CMakeLists.txt << 'EOF'
          smtg_add_vst3plugin(SinDecomposer
              src/plugprocessor.cpp
              src/plugcontroller.cpp
              src/plugfactory.cpp
              src/editor.cpp
              src/fft.cpp
          )
          target_include_directories(SinDe
